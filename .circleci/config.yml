version: 2
jobs:
#   build:
#     docker:
#       - image: circleci/<language>:<version TAG>
#     steps:
#       - checkout
#       - run: <command>
  test:
    steps:
      - checkout
      - run:
          name: Test
          command: .././test/test.js
workflows:
  version: 2
  build_and_test:
    jobs:
#       - build
      - test
